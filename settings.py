# App settings — non-secret configuration.
# Edit this file to change app behaviour without touching secrets.toml.

# LLM backend to use.
# "mistral"   — always use Mistral (hides toggle)
# "anthropic" — always use Anthropic (hides toggle)
# "toggle"    — show the toggle switch so users can choose
BACKEND = "toggle"
